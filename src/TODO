Installation:
remarkable + fix for mathjax
pip3 install mkdocs-material


after mkdocs build:
use rsync to move from site to root dir
commit to git
push

------------------------------
#!/bin/bash

mkdocs build
rsync -avcn site/ ../ --exclude=.* --exclude=src --exclude=README.md --delete
#rsync -avc site/ ../ --exclude=.* --exclude=src --exclude=README.md --delete
git add -A && git commit -m "Update pages"
git push origin master

